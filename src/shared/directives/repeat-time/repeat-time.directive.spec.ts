import { RepeatTimeDirective } from "./repeat-time.directive";

describe('RepeatTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatTimeDirective();
    expect(directive).toBeTruthy();
  });
});
